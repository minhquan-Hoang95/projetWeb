{% extends 'Layouts/layout_vente.html.twig' %}

{% block title %}
    Edit User
{% endblock %}

{% block css %}
    <link rel="stylesheet" href="{{ asset('css/admin.css') }}">
{% endblock %}

{% block vue %}
    <h1>Edit User I'm here</h1>
    {% if users|length == 0 %}
        <h3>Il n'y a pas de user</h3>
    {% else %}
        {#{% for user in users %}
            <ul>
                <li>{{ user.login }}</li>
                <li>{{ user.firstname }}</li>
                <li>{{ user.lastname }}</li>
                <li>{{ user.password }}</li>
                {% for role in user.roles %}
                    {% if role != 'ROLE_NOROLE' %}
                        <li>{{ role }}</li>
                    {% endif %}
                {% endfor %}
                <li>{{ user.pays.name }}</li>
            </ul>
        {% endfor %}#}
        <table>
        <tr>
            <th>Login</th>
            <th>First Name</th>
            <th>Last Name</th>
            <th>Password</th>
            <th>Roles</th>
            <th>Pays</th>
            <th>Action</th>
        </tr>
        {% for user in users %}

                <tr>
                    <td>{{ user.login }}</td>
                    <td>{{ user.firstname }}</td>
                    <td>{{ user.lastname }}</td>
                    <td>{{ user.password }}</td>
                    <td>
                            {% for role in user.roles %}
                                {% if role != 'ROLE_NOROLE' %}
                                    {{ role }}
                                {% endif %}
                            {% endfor %}
                    </td>
                    <td>{{ user.pays.name }}</td>
                    <td>
                        <a href="{{ path('admin_delete', {'id' : user.id}) }}">Supprimer</a>

                    </td>
                </tr>
        {% endfor %}

        </table>
    {% endif %}

    {{ dump(users) }}

{% endblock %}




